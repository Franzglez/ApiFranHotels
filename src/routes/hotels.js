const { Router } = require('express');
const router = Router();
const usuarios = require ('../../Data/Hotels.json')

//Raiz
router.post('/hotel', (req, res) => {
    res.send(
        
        
    );
})

module.exports = router;